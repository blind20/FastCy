package com.xs.fastcy.cypda.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.xs.fastcy.cypda.entity.User;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "tm_user".
*/
public class UserDao extends AbstractDao<User, Long> {

    public static final String TABLENAME = "tm_user";

    /**
     * Properties of entity User.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property UserId = new Property(0, Long.class, "userId", true, "_id");
        public final static Property Username = new Property(1, String.class, "username", false, "USERNAME");
        public final static Property Password = new Property(2, String.class, "password", false, "PASSWORD");
        public final static Property Sfzmhm = new Property(3, String.class, "sfzmhm", false, "SFZMHM");
        public final static Property Yhxm = new Property(4, String.class, "yhxm", false, "YHXM");
        public final static Property Glbm = new Property(5, String.class, "glbm", false, "GLBM");
        public final static Property Bmmc = new Property(6, String.class, "bmmc", false, "BMMC");
        public final static Property IsLoginTag = new Property(7, boolean.class, "isLoginTag", false, "IS_LOGIN_TAG");
    }


    public UserDao(DaoConfig config) {
        super(config);
    }
    
    public UserDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"tm_user\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: userId
                "\"USERNAME\" TEXT," + // 1: username
                "\"PASSWORD\" TEXT," + // 2: password
                "\"SFZMHM\" TEXT," + // 3: sfzmhm
                "\"YHXM\" TEXT," + // 4: yhxm
                "\"GLBM\" TEXT," + // 5: glbm
                "\"BMMC\" TEXT," + // 6: bmmc
                "\"IS_LOGIN_TAG\" INTEGER NOT NULL );"); // 7: isLoginTag
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"tm_user\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, User entity) {
        stmt.clearBindings();
 
        Long userId = entity.getUserId();
        if (userId != null) {
            stmt.bindLong(1, userId);
        }
 
        String username = entity.getUsername();
        if (username != null) {
            stmt.bindString(2, username);
        }
 
        String password = entity.getPassword();
        if (password != null) {
            stmt.bindString(3, password);
        }
 
        String sfzmhm = entity.getSfzmhm();
        if (sfzmhm != null) {
            stmt.bindString(4, sfzmhm);
        }
 
        String yhxm = entity.getYhxm();
        if (yhxm != null) {
            stmt.bindString(5, yhxm);
        }
 
        String glbm = entity.getGlbm();
        if (glbm != null) {
            stmt.bindString(6, glbm);
        }
 
        String bmmc = entity.getBmmc();
        if (bmmc != null) {
            stmt.bindString(7, bmmc);
        }
        stmt.bindLong(8, entity.getIsLoginTag() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, User entity) {
        stmt.clearBindings();
 
        Long userId = entity.getUserId();
        if (userId != null) {
            stmt.bindLong(1, userId);
        }
 
        String username = entity.getUsername();
        if (username != null) {
            stmt.bindString(2, username);
        }
 
        String password = entity.getPassword();
        if (password != null) {
            stmt.bindString(3, password);
        }
 
        String sfzmhm = entity.getSfzmhm();
        if (sfzmhm != null) {
            stmt.bindString(4, sfzmhm);
        }
 
        String yhxm = entity.getYhxm();
        if (yhxm != null) {
            stmt.bindString(5, yhxm);
        }
 
        String glbm = entity.getGlbm();
        if (glbm != null) {
            stmt.bindString(6, glbm);
        }
 
        String bmmc = entity.getBmmc();
        if (bmmc != null) {
            stmt.bindString(7, bmmc);
        }
        stmt.bindLong(8, entity.getIsLoginTag() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public User readEntity(Cursor cursor, int offset) {
        User entity = new User( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // userId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // username
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // password
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // sfzmhm
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // yhxm
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // glbm
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // bmmc
            cursor.getShort(offset + 7) != 0 // isLoginTag
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, User entity, int offset) {
        entity.setUserId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUsername(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setPassword(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSfzmhm(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setYhxm(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setGlbm(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setBmmc(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setIsLoginTag(cursor.getShort(offset + 7) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(User entity, long rowId) {
        entity.setUserId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(User entity) {
        if(entity != null) {
            return entity.getUserId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(User entity) {
        return entity.getUserId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
